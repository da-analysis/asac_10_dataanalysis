"""kg당 단가의 공통 상·하한.

네이버 검색(search_backends), LLM 추정(missing_price_search), 원가 계산
(cost_calculator)이 제각각 다른 상한을 쓰면 "한쪽은 통과시켰는데 다른 쪽이
다시 버리는" 불일치가 생긴다. 기준을 여기 한 곳에 모아 공유한다.
"""

# kg당 단가 상한. 이보다 비싸면 검색이 엉뚱한 상품(소량 조미료, 수입 가공품
# 등)을 잡아 단위 환산을 잘못한 것으로 보고 가격을 버린다.
# (예: '김칫국물 소금맛 68g' → ₩112,000/kg 같은 케이스 방지)
MAX_PRICE_PER_KG = 100_000

# kg당 단가 하한. 이보다 싸면 단위 환산 오류(예: 대용량을 소량으로 착각)로 본다.
MIN_PRICE_PER_KG = 100
